pub fn usage() {
    println!("Usage: ");
    println!("rrs [command]");
    println!("  command:");
    println!("    run: run the server.");
    println!("    build: foreach the src and build to generate in target.");
}
